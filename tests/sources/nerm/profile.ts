import { SOURCES } from '../../config/sources';

export const NERM_SOURCE = SOURCES.nerm;

/** NERM account field listed on the QA PDFs. Design doc: Legal Hold applies; Manager Hold does not. */
export const NERM_HOLD_ACCOUNT_FIELDS = ['Legal_Hold'];
